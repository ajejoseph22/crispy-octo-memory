import React from 'react'
import styled from 'styled-components'

const DateAreaWrapper = styled.div`
  width: 25%;
  background: pink;
  float: left;
  height: 100vh;
`

const DateArea: React.FC = () => {
  return <DateAreaWrapper />
}

export default DateArea
